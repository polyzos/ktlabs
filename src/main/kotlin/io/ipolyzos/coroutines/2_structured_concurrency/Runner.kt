package io.ipolyzos.coroutines.`2_structured_concurrency`

/**
 * Jobs
 *
 * Contexts:
 *  provide a context in which to run suspend functions
 *
 * Scopes:
 *  project a 'scope' for suspend functions
 *  scope allows for a degree of control - cancellation, error handling
 * */
fun main() {}